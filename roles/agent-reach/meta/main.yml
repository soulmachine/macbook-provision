---
dependencies:
  # The role drives the `claude` binary headlessly to run upstream's agent-facing
  # install/update docs.
  - claude-code
  # agent-reach is a Python package; install.md's recommended path is
  # `pipx install`, with a venv fallback. Both need the provisioned Python.
  - python
  # Core install pulls npm-based upstream tools (mcporter, and opencli on desktop).
  - nodejs
  # `gh` is one of the zero-config channels agent-reach activates on install.
  - github
