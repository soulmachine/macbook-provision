---
- name: Install oh-my-zsh
  ansible.builtin.shell: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended
  args:
    creates: "{{ lookup('env', 'HOME') }}/.oh-my-zsh"

- name: Check current login shell
  ansible.builtin.shell: dscl . -read ~ UserShell | awk '{print $2}'
  register: current_shell
  changed_when: false

- name: Set zsh as default shell
  ansible.builtin.shell: dscl . -change ~ UserShell {{ current_shell.stdout }} /bin/zsh
  when: current_shell.stdout != '/bin/zsh'

- name: Install zsh-autosuggestions
  ansible.builtin.git:
    repo: https://github.com/zsh-users/zsh-autosuggestions
    dest: "{{ lookup('env', 'HOME') }}/.oh-my-zsh/custom/plugins/zsh-autosuggestions"
    update: no

- name: Check if zsh-autosuggestions plugin is enabled in .zshrc
  ansible.builtin.shell: grep '^plugins=' "{{ lookup('env', 'HOME') }}/.zshrc" | grep -qE '(\(|[[:space:]])zsh-autosuggestions([[:space:]]|\))'
  register: zsh_autosuggestions_check
  changed_when: false
  failed_when: false

- name: Enable zsh-autosuggestions oh-my-zsh plugin
  ansible.builtin.shell: sed -i '' 's/^plugins=(\(.*\))/plugins=(\1 zsh-autosuggestions)/' "{{ lookup('env', 'HOME') }}/.zshrc"
  when: zsh_autosuggestions_check.rc != 0
