[defaults]
nocows = True
inventory = ./inventory
roles_path = ./roles
callback_result_format = yaml

[privilege_escalation]
# Use sudo -A so become falls through to SUDO_ASKPASS (configured by bootstrap.sh),
# avoiding the need to prime credentials with `sudo -v` before each run.
become_method = sudo
become_flags = -H -A
