[defaults]
nocows = True
inventory = ./inventory
roles_path = ./roles
callback_result_format = yaml

[privilege_escalation]
# Passwordless sudo (a /etc/sudoers.d drop-in configured by bootstrap.sh) means
# become needs no password mechanism — just -H to set root's HOME.
become_method = sudo
become_flags = -H
