brew_info/
roles/elliotweiser.osx-command-line-tools/
.env
.serena/

# Regenerated by Codex's interactive `/import` — a string-replaced copy of
# CLAUDE.md, headed "guidance to Codex (Codex.ai/code)". It was removed from the
# repo deliberately (b073f8e) and keeps reappearing, so ignore it rather than
# re-deleting it every time. A real, hand-written AGENTS.md can still be
# committed with `git add -f`.
AGENTS.md

# Same tooling: a byte-identical duplicate of the TRACKED
# .claude/skills/ansible-idempotency-check/SKILL.md, which is the canonical copy
# and the one CLAUDE.md refers to.
.agents/

